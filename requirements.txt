fastapi==0.136.1
uvicorn==0.46.0
pandas==2.2.3
numpy>=1.24.0
python-dotenv==1.1.0
fastavro==1.12.2
google-cloud-storage==3.4.0
# Imported directly to verify Pub/Sub push tokens, so pin it rather than
# relying on it staying a transitive dependency of google-cloud-storage.
google-auth==2.39.0
# Runtime state store. The REST client, not a TCP one: a serverless
# function may cold-start on any request, and a TCP handshake plus auth
# per invocation costs more than a call to a pooled HTTPS endpoint.
upstash-redis==1.7.0
requests==2.31.0
pydantic==2.11.3
python-multipart>=0.0.9
aiofiles>=23.0.0
